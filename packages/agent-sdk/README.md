# Agent SDK

The Agent SDK provides the common foundation for every AI agent in StoryForge AI.

Responsibilities:

- Logging
- Validation
- Shared Memory access
- Prompt loading
- Metrics
- Retry handling

Every agent extends BaseAgent.

Current Version:
v0.1